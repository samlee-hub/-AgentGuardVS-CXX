Implement the task: insufficient resources during summon must return a clear ERROR, and the client should display that error clearly.

Allowed files:
- `Server1/Server1/Server/StrategyBattleMode.cpp`
- `Client1/Client1/Client/Client.cpp`

Forbidden files:
- `Server1/Server1/Server/DataManager.cpp`
- `Server1/Server1/Server/MatchManager.cpp`
- `Server1/Server1/Server/GameModeFactory.cpp`
- `Server1/Server1/Server/Server.vcxproj`

First output a PatchPlan. All edits must stay in allowed files. If another file is required, stop and explain. Build after editing if possible, and repair failures only inside allowed files. End with summary, build result, and risk notes.
