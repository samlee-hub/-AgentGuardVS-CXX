Implement the task: battle records and user result updates must be settled at most once per battle.

Allowed files:
- `Server1/Server1/Server/BattleRoom.cpp`
- `Server1/Server1/Server/BattleRoom.h`

Forbidden files:
- `Server1/Server1/Server/DataManager.cpp`
- `Server1/Server1/Server/UserManager.cpp`
- `Server1/Server1/Server/Logger.cpp`
- `Server1/Server1/Server/StrategyBattleMode.cpp`

First output a PatchPlan. Modify only allowed files. If another file is required, stop and explain. Build after editing if possible. Repair failures only inside allowed files. Finish with summary, build result, and risks.
