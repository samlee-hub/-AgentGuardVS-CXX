Implement the task: prevent duplicate login or duplicate online-user insertion for the same user.

Allowed files:
- `Server1/Server1/Server/UserManager.cpp`
- `Server1/Server1/Server/UserManager.h`
- `Server1/Server1/Server/Server.cpp`

Forbidden files:
- `Server1/Server1/Server/DataManager.cpp`
- `Server1/Server1/Server/MatchManager.cpp`
- `Server1/Server1/Server/BattleRoom.cpp`
- `Client1/Client1/Client/Client.cpp`

First output a PatchPlan. Modify only allowed files. If another file is required, stop and explain. Build after editing if possible. Repair build failures only inside allowed files. Finish with summary, build result, and risks.
