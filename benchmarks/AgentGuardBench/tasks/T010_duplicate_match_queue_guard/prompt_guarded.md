Implement the task: prevent duplicate match queue entries for the same player/socket and preserve clear waiting/already-in-room responses.

Allowed files:
- `Server1/Server1/Server/MatchManager.cpp`
- `Server1/Server1/Server/MatchManager.h`
- `Server1/Server1/Server/Server.cpp`

Forbidden files:
- `Server1/Server1/Server/BattleRoom.cpp`
- `Server1/Server1/Server/StrategyBattleMode.cpp`
- `Server1/Server1/Server/UserManager.cpp`
- `Client1/Client1/Client/Client.vcxproj`

First output a PatchPlan. Modify only allowed files. If another file is required, stop and explain. Build after editing if possible. Repair only in allowed files. Finish with summary, build result, and risks.
